# 隐藏的 1.31+ 原生 C 语言盲转通道
location @native_tunnel {
    # [安全审计] 只有经过 Lua 验证的合法请求才能进入此块，故在此开启物理放行
    allow all;

    # 🎯 激活 Nginx 1.31+ 官方原生黑科技！
    tunnel_pass;

    # 调优商用级传输参数，最大限度消灭时延抖动指纹
    tunnel_buffer_size 64k;
    tunnel_connect_timeout 15s;
    tunnel_socket_keepalive on;
}
