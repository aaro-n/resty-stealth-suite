# 🔑 Cloudflare 专属客户端证书强校验 (mTLS / Authenticated Origin Pulls)
# 作用：限制此虚拟主机仅允许携带 Cloudflare 官方客户端证书的请求建立连接，防止直接暴露源站。

# 启用强制客户端证书校验，不通过直接拒绝
ssl_verify_client on;

# 引入 Cloudflare 官方颁发的 CA 根证书（用于验证 CF 传入的客户端证书）
ssl_client_certificate /etc/nginx/certs/cloudflare-origin-pull-ca.pem;
