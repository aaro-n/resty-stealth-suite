# RestyTunnel SSL 证书存放目录

此目录用于存放本地自签名或生产环境的 SSL 证书。

## 目录结构
通常，以下文件会被放置在此处：
- `fullchain.pem` - 公钥证书
- `privkey.pem` - 证书私钥

## 安全说明
为了防止密钥泄露，生产环境的私钥和证书（如 `*.pem`、`*.key`）**严禁**提交到公共代码仓库。本目录下的 `.pem` 和 `.key` 文件已通过 `.gitignore` 自动忽略。
